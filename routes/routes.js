// const characterRoutes = require('./characters');

// const appRouter = (app, fs) => {
//     app.get('/', (req, res) => {
//         res.send('welcome to the lara development api-server');
//         res.send(characterRoutes(app, fs));
//     });
// };

// module.exports = appRouter;